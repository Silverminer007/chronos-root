# --- OIDC / Keycloak ---
quarkus.oidc.auth-server-url=${AUTH_SERVER_BASE_URL:http://localhost:8080}/realms/${AUTH_SERVER_REALM:master}
quarkus.oidc.client-id=${AUTH_SERVER_CLIENT_ID}
quarkus.oidc.credentials.secret=${AUTH_SERVER_CLIENT_SECRET}
quarkus.oidc.roles.role-claim-path=realm_access/roles

# --- Keycloak Admin Client ---
quarkus.keycloak.admin-client.server-url=${AUTH_SERVER_BASE_URL}
quarkus.keycloak.admin-client.realm=${AUTH_SERVER_REALM:master}
quarkus.keycloak.admin-client.client-id=${AUTH_SERVER_CLIENT_ID}
quarkus.keycloak.admin-client.client-secret=${AUTH_SERVER_CLIENT_SECRET}
quarkus.keycloak.admin-client.grant-type=CLIENT_CREDENTIALS

# --- Datenbank (Postgres) ---
quarkus.datasource.db-kind=postgresql
%prod.quarkus.datasource.username=${DB_USER}
%prod.quarkus.datasource.password=${DB_PASSWORD}
%prod.quarkus.datasource.jdbc.url=${DB_URL}
%dev.quarkus.datasource.username=${DB_USER:chronos}
%dev.quarkus.datasource.password=${DB_PASSWORD:chronos} # pragma: allowlist secret
%dev.quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/chronos}

# --- Connection Pool (Agroal) ---
quarkus.datasource.jdbc.min-size=10
quarkus.datasource.jdbc.max-size=50
quarkus.datasource.jdbc.acquisition-timeout=30S
quarkus.datasource.jdbc.validation-query-sql=SELECT 1
quarkus.datasource.jdbc.idle-removal-interval=PT2M
quarkus.datasource.jdbc.max-lifetime=PT15M

quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.baseline-version=1.0.0
quarkus.flyway.locations=db/migration

push.vapid.mailto=mailto:${VAPID_MAILTO}
push.vapid.public=${VAPID_PUBLIC}
push.vapid.private=${VAPID_PRIVATE}

quarkus.live-reload.instrumentation=true

# Micrometer aktivieren
quarkus.micrometer.enabled=true

# Prometheus-Exporter einschalten
quarkus.micrometer.export.prometheus.enabled=true
quarkus.micrometer.export.prometheus.path=/metrics

# HTTP-Server Metrics aktivieren (Standard)
quarkus.micrometer.binder.http-server.enabled=true
# HTTP Metrics aktivieren, aber bestimmte URIs ignorieren
quarkus.micrometer.binder.http-server.ignore-patterns=^\/metrics$
quarkus.micrometer.binder.http-client.suppress4xx-errors=true

# File Logging aktivieren
quarkus.log.file.enabled=true
quarkus.log.file.path=./logs/application.log
quarkus.log.file.format=%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n

# Rotation konfigurieren
quarkus.log.file.rotation.max-backup-index=10
quarkus.log.file.rotation.file-suffix=.yyyy-MM-dd
quarkus.log.file.rotation.rotate-on-boot=true

quarkus.management.enabled=true

# --- Leader Election (Kubernetes Lease) ---
scheduler.leader-election.enabled=${LEADER_ELECTION_ENABLED:true}
