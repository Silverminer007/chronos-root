namespace: chronos-staging

secrets:
  organisationId: 3e4b7901-bc75-4688-9a82-b3f600a40fa8
  db:
    password: a1efa257-6675-437e-ad17-b3f700cf6878
  backend:
    clientId: 6f7e4147-4c30-408c-be5a-b3f700cf12f4
    clientSecret: 22e2fb2f-83ca-4ce6-bf74-b3f700cf28ff
  frontend:
    clientId: a71d209e-4600-4eaa-a81f-b3f700cf38cb
  vapid:
    publicKey: aefad867-54b7-4764-9f12-b3f600b42da2
    privateKey: fa639799-a9ef-465a-81c1-b3f600b4454b
    mailto: 0f9266a6-9989-424a-9d91-b3f600b45aa1
  imagePullSecret: f02d5821-dfeb-4b53-8611-b3f600aeefde

ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  host: staging.chronos-live.de
  tls:
    secretName: chronos-tls-cert

frontend:
  replicaCount: 1
  autoscaling:
    enabled: false
    minReplicas: 2
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
  image:
    repository: ghcr.io/silverminer007/chronos-nuxt
    tag: develop
    pullPolicy: Always
  containerPort: 3000
  service:
    type: ClusterIP
    port: 80
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "200m"
  env:
    NUXT_AUTH_ISSUER: "https://auth.chronos-live.de/realms/chronos"
    NUXT_AUTH_REDIRECT_URI: https://staging.chronos-live.de/api/auth/callback
    NUXT_QUARKUS_URL: "http://chronos-backend"
    NUXT_PUBLIC_IMPRESSUM_NAME: "Justus Henze"
    NUXT_PUBLIC_IMPRESSUM_STREET: "Malmedystraße 3"
    NUXT_PUBLIC_IMPRESSUM_CITY: "46145 Oberhausen"
    NUXT_PUBLIC_IMPRESSUM_EMAIL: "henze5@gmx.de"
  envFromSecrets:
    - name: NUXT_AUTH_CLIENT_ID
      secretName: chronos-secret
      secretKey: frontend_client_id

backend:
  replicaCount: 1
  autoscaling:
    enabled: false
    minReplicas: 2
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
  image:
    repository: ghcr.io/silverminer007/chronos-date-api
    tag: develop
    pullPolicy: Always
  containerPort: 8080
  service:
    type: ClusterIP
    port: 80
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "200m"
  env:
    DB_URL: "jdbc:postgresql://postgres:5432/chronos"
    DB_USER: "postgres"
    AUTH_SERVER_URL: "https://auth.chronos-live.de/realms/chronos"
  envFromSecrets:
    - name: DB_PASSWORD
      secretName: chronos-secret
      secretKey: database_password
    - name: AUTH_SERVER_CLIENT_ID
      secretName: chronos-secret
      secretKey: backend_client_id
    - name: AUTH_SERVER_CLIENT_SECRET
      secretName: chronos-secret
      secretKey: backend_client_secret
    - name: VAPID_PUBLIC
      secretName: chronos-secret
      secretKey: vapid_public_key
    - name: VAPID_PRIVATE
      secretName: chronos-secret
      secretKey: vapid_private_key
    - name: VAPID_MAILTO
      secretName: chronos-secret
      secretKey: vapid_mailto
  livenessProbe:
    httpGet:
      path: /q/health/live
      port: 9000
    initialDelaySeconds: 30
    periodSeconds: 10
  readinessProbe:
    httpGet:
      path: /q/health/ready
      port: 9000
    initialDelaySeconds: 5
    periodSeconds: 5

postgresql:
  enabled: true
  image:
    tag: "16"
  database: chronos
  storage:
    size: 10Gi
    storageClass: hcloud-volumes
  passwordSecret:
    name: chronos-secret
    key: database_password
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"

metrics:
  enabled: true
  port: 9000
  targetPort: 9000
  path: /metrics
  serviceMonitor:
    enabled: true
    interval: 30s
    scrapeTimeout: 10s

alerts:
  enabled: true
  rules:
    podDown:
      for: 2m
    postgresDown:
      for: 1m
    highCPU:
      threshold: 0.8
      for: 5m
    highMemory:
      threshold: 0.9
      for: 5m
    postgresDiskFull:
      threshold: 0.85
      for: 2m
    podRestarting:
      threshold: 3
    sslExpiry:
      daysWarning: 7